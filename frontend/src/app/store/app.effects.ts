import { Injectable } from '@angular/core';

/** Register with `provideEffects([AppEffects])` when you add `createEffect` calls. */
@Injectable()
export class AppEffects {
  // App-wide side effects (HTTP, navigation, toasts) go here.
}
